Implement De/Ser traits on:
- char
- Arrays (use const-generics to handle [T; N])
- Tuples (up to 8 parameters)
- String
- HashMap / HashSet
- Vec / VecDeque
- Option
- Box
- Structs with De/Ser fields
- Enums with De/Ser fields
- Bounded integers with specific ranges using const generics (define min & max w/ i64s)

Completed:
- Boolean
- Unit Type: ()
- Unsigned integers: u8, u16, u32, u64, usize
- Signed integers: i8, i16, i32, i64, isize
- Floating point: f32, f64