[package]
name = "naia-macroquad-client-demo"
authors = ["connorcarpenter <connorcarpenter@gmail.com>"]
version = "0.1.0"
edition = "2018"
license = "MIT OR Apache-2.0"

[[bin]]
name = "naia-macroquad-client-demo"
path = "src/main.rs"

[features]

[dependencies]
naia-macroquad-demo-shared = { path = "../shared" }
naia-client = { path = "../../../client", features = [ "mquad" ] } #version = "0.5.0", features = [ "mquad" ] }
cfg-if = "0.1.10"
macroquad = { version = "=0.3.0-alpha.12", features = [ "log-impl" ] }